name: SomaAI

options:
  bundleIdPrefix: com.olegkonovalov
  deploymentTarget:
    iOS: 17.0

settings:
  base:
    SWIFT_VERSION: 5.9
    GENERATE_INFOPLIST_FILE: YES

configurations:
  Debug: debug
  Release: release

targets:
  SomaAI:
    type: application
    platform: iOS
    sources:
      - path: Sources/App
      - path: Sources/Config
      - path: Sources/Models
      - path: Sources/Services
      - path: Sources/Views
    resources:
      - path: LaunchScreen.storyboard
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.oleg.SomaAI
        TARGETED_DEVICE_FAMILY: "1"
        SUPPORTS_MACCATALYST: NO
        SUPPORTED_PLATFORMS: iphoneos iphonesimulator
        INFOPLIST_KEY_UILaunchStoryboardName: LaunchScreen
        INFOPLIST_KEY_NSCameraUsageDescription: "Soma AI uses the camera to scan medical documents for your personal Health Passport."
        INFOPLIST_KEY_NSPhotoLibraryUsageDescription: "Soma AI accesses photos so you can select medical documents to add to your Health Passport."
        INFOPLIST_KEY_NSMicrophoneUsageDescription: "Soma AI does not record audio."
        CODE_SIGN_STYLE: Automatic
